{
  "name": "vector-server",
  "version": "1.0.0",
  "description": "Vector memory MCP server — pgvector + OpenAI embeddings",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "test": "vitest run",
    "setup-db": "node setup-db.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "pg": "^8.13.0"
  },
  "devDependencies": {
    "vitest": "^4.1.0"
  }
}
