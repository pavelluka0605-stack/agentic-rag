{
  "name": "memory-server",
  "version": "2.0.0",
  "description": "Multi-layer development memory MCP server (SQLite + FTS5)",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "test": "vitest run"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "better-sqlite3": "^12.0.0"
  },
  "devDependencies": {
    "vitest": "^4.1.0"
  }
}
